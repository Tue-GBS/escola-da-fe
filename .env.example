# .env.example - Arquivo de exemplo com valores SEGUROS apenas
# ⚠️ NUNCA COMMITAR dados sensíveis ou valores reais neste arquivo
# Copie para .env e preencha com seus valores reais

# ⚠️ BASE_URL: Ajuste conforme o ambiente de deploy
# Localhost (padrão): BASE_URL=/
# Subpasta: BASE_URL=/escola-da-fe/
# Domínio customizado: BASE_URL=https://seu-dominio.com/
BASE_URL=/

# Ambiente da aplicação (development, staging, production)
APP_ENV=development

# Debug (true apenas em desenvolvimento, NUNCA em produção)
DEBUG=false

# Docker
IMAGE_NAME=escola-da-fe:latest
IMAGE_TAG=latest

# ===== API Keys (preencha em .env, NUNCA aqui com valores reais) =====
# LITURGIA_API_KEY=your-api-key-here
# API_ENDPOINT=https://liturgia.up.railway.app/v2/

# ===== Banco de dados (se usado) =====
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=escola_da_fe
# DB_USER=app_user
# DB_PASSWORD=your-db-password-here

# ===== Email (se usado para contatos/formulários) =====
# MAIL_HOST=smtp.seumail.com
# MAIL_PORT=587
# MAIL_USERNAME=seu-email@dominio.com
# MAIL_PASSWORD=your-email-password-here
# MAIL_FROM=noreply@escola-da-fe.com

# ===== Tokens e credenciais sensíveis =====
# ⚠️ NUNCA commite tokens reais neste arquivo
# Use variáveis de ambiente em produção (GitHub Secrets, Docker, etc.)
# ADMIN_TOKEN=your-admin-token-here
# API_SECRET=your-api-secret-here
# JWT_SECRET=your-jwt-secret-here

# ===== Proxy / CORS (se necessário) =====
# CORS_ORIGIN=https://seu-dominio.com
# HTTP_PROXY=http://proxy.empresa.com:8080

# ===== Boas práticas de segurança =====
# ✅ 1. Copie este arquivo: cp .env.example .env
# ✅ 2. Preencha .env com seus valores REAIS (nunca commite)
# ✅ 3. Mantenha .env.example com valores de exemplo SEGUROS
# ✅ 4. Use .gitignore para proteger .env (já configurado)
# ✅ 5. Em produção, use:
#       - GitHub Secrets para CI/CD
#       - Docker environment variables
#       - Serviços de gerenciamento de secrets (AWS Secrets Manager, etc.)
#       - Variáveis de sistema do servidor
